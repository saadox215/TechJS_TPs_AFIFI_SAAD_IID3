doctype html
html(lang="fr")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title= title || "Express Auth TP"
    // Tailwind CDN (rapide pour dev)
    script(src="https://cdn.tailwindcss.com")
    // Optionnel : config minimale pour Tailwind (ex : ajouter une couleur)
    script.
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              brand: {
                DEFAULT: '#0ea5a4', // teal-ish
                dark: '#0891b2'
              }
            }
          }
        }
      }
    // Google Fonts (facultatif)
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet")
    style.
      html, body { font-family: 'Inter', system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; }
  body(class="min-h-screen bg-gradient-to-br from-gray-50 via-white to-slate-50")
    // Top nav
    header(class="max-w-4xl mx-auto py-6 px-4 flex items-center justify-between")
      h1(class="text-2xl font-bold text-slate-800") Express Auth — TP - AFIFI Saad IID3
      if user
        div(class="flex items-center gap-4")
          span(class="text-sm text-slate-600") Bonjour #[strong #{user.username}]
          a(href="/logout" class="px-3 py-2 bg-red-500 hover:bg-red-600 text-white rounded-md text-sm") Déconnexion
      else
        nav(class="flex gap-3")
          a(href="/login" class="text-sm text-slate-700 hover:text-brand") Connexion
          a(href="/register" class="text-sm text-slate-700 hover:text-brand") Inscription

    main(class="max-w-4xl mx-auto px-4 py-8")
      block content

    footer(class="max-w-4xl mx-auto px-4 py-8 text-center text-sm text-slate-500")
      | Développé par Saad AFIFI
